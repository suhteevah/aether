// expect: exit=42
// Mul + Sub + Div on f64, then a compare picks the exit code.
//   ((10.0 * 4.5) - 3.0) / 1.0 = 42.0
//   42.0 > 41.5 → true → return 42
fn main() -> i32 {
    let a: f64 = 10.0;
    let b: f64 = 4.5;
    let prod: f64 = a * b;
    let diff: f64 = prod - 3.0;
    let q: f64 = diff / 1.0;
    if q > 41.5 { 42 } else { 0 }
}
