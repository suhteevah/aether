// expect: exit=42
// expect: stdout contains computing
// Locals + arithmetic; tail expression becomes the exit code.
fn main() -> i32 {
    let x = 7;
    let y = 35;
    let z = x + y;
    println("computing 7 + 35:");
    z
}
