// expect: exit=42
// roadmap: P13.1
// `mut` keyword permitted on fn parameter; body mutates it.
fn add_one(mut x: i64) -> i64 {
    x = x + 1;
    x
}

fn main() -> i32 {
    let r = add_one(41);
    r as i32
}
