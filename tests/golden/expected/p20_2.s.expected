# AETHER x86-64 assembly (Microsoft x64 ABI)
# Emitted by aetherc; comments here are debug-only and do not
# come from any .aether source — those were stripped at lex time.

.section .text
.globl main

aether_sum_to:
    pushq %rbp
    movq %rsp, %rbp
    subq $64, %rsp
    movq %rcx, -8(%rbp)
    movq $0, -24(%rbp)
    movq $1, -32(%rbp)
.L_sum_to_while_top_0:
    movq -32(%rbp), %rax
    pushq %rax
    movq -8(%rbp), %rax
    popq %r10
    xchgq %rax, %r10
    cmpq %r10, %rax
    setle %al
    movzbl %al, %eax
    testq %rax, %rax
    je .L_sum_to_while_end_1
    movq -24(%rbp), %rax
    pushq %rax
    movq -32(%rbp), %rax
    popq %r10
    xchgq %rax, %r10
    addq %r10, %rax
    movq %rax, -24(%rbp)
    movq -32(%rbp), %rax
    pushq %rax
    movq $1, %rax
    popq %r10
    xchgq %rax, %r10
    addq %r10, %rax
    movq %rax, -32(%rbp)
    jmp .L_sum_to_while_top_0
.L_sum_to_while_end_1:
    xorl %eax, %eax
    movq -24(%rbp), %rax
    addq $64, %rsp
    popq %rbp
    ret
    xorl %eax, %eax
    addq $64, %rsp
    popq %rbp
    ret

main:
    pushq %rbp
    movq %rsp, %rbp
    subq $48, %rsp
    movq $8, %rax
    subq $16, %rsp
    movq %rax, (%rsp)
    movq 0(%rsp), %rax
    movq %rax, %rcx
    addq $16, %rsp
    callq aether_sum_to
    pushq %rax
    movq $6, %rax
    popq %r10
    xchgq %rax, %r10
    addq %r10, %rax
    addq $48, %rsp
    popq %rbp
    ret

