[workspace]
members = ["compiler", "runtime", "runtime_pe", "trainer", "aether_asm", "tools/audit"]
resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
